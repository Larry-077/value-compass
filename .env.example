# 复制为 .env 填入令牌即可全站实时运行。不填也能用：每页默认显示作者已固化的真实结果。

# ── 最简：一个智谱 GLM 令牌就够（生成器 + 评审 都用 GLM，免费）──
# 申请：https://open.bigmodel.cn/  → API Keys
OPENAI_BASE_URL=https://open.bigmodel.cn/api/paas/v4/
OPENAI_API_KEY=填入你的智谱GLM令牌
VC_MODEL=glm-4-flash

# 后端端口
PORT=8787

# ── 可选：想让"评审/批评"更强，可单独给评审接更强的模型 ──
# 例如评审用更强的 GLM 或其它 OpenAI 兼容模型（不填则评审也用上面的 glm-4-flash）
# VC_JUDGE_BASE_URL=https://open.bigmodel.cn/api/paas/v4/
# VC_JUDGE_API_KEY=填入令牌
# VC_JUDGE_MODEL=glm-4-plus
