# 复制为 .env 填入你的令牌。不填也能运行：后端自动回退到内置 mock。

# ── 默认 provider（OpenAI 兼容）。生成器/评审未单独配置时都用它 ──
OPENAI_BASE_URL=https://apicz.boyuerichdata.com/v1/
OPENAI_API_KEY=sk-你的令牌
VC_MODEL=claude-sonnet-4-6

# ── 分离「生成器(被对齐的弱策略)」与「评审(强老师/奖励)」──────────
# 推荐：生成器用国产开源弱模型(SiliconFlow)，评审用强模型。
# 拿到 SiliconFlow key 后，取消下面注释即可：
# VC_GEN_BASE_URL=https://api.siliconflow.cn/v1
# VC_GEN_API_KEY=sk-你的SiliconFlow令牌
# VC_GEN_MODEL=Qwen/Qwen2.5-7B-Instruct          # 或 THUDM/GLM-4-9B-Chat
#
# VC_JUDGE_BASE_URL=https://api.siliconflow.cn/v1  # 评审也可继续用上面的默认中转
# VC_JUDGE_API_KEY=sk-你的SiliconFlow令牌
# VC_JUDGE_MODEL=Qwen/Qwen2.5-72B-Instruct        # 或 deepseek-ai/DeepSeek-V3

# 后端端口
PORT=8787
